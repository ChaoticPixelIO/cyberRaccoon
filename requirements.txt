# Core dependencies
# NOTE: opencv-python intentionally excluded — Pi venv inherits system
# python3-opencv (with GStreamer). pip-installing opencv-python shadows
# it and breaks AirPlay capture. Install via: sudo apt install python3-opencv
Pillow>=10.0.0
numpy>=1.24.0

# LLM providers
anthropic>=0.45.0
openai>=1.66.0

# Config persistence
pyyaml>=6.0

# CLI REPL (optional, falls back to basic input())
prompt_toolkit>=3.0

# Web UI
fastapi>=0.110.0
uvicorn>=0.34.0
websockets>=13.0,<14  # uvicorn (even 0.39) uses websockets.legacy — broken in v14+

# Development
pytest>=8.0.0
httpx>=0.27.0
